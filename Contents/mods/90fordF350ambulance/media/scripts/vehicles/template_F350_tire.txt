module Base
{

	model 90fordF350ambulanceWheel
	{
		mesh = vehicles/Vehicles_90fordF350ambulance_Wheels|f350_tire,
		texture = vehicles/Vehicles_90fordF350ambulance_Wheel,
		shader = vehiclewheel,
		scale = 0.97,
	}

	model 90fordF350ambulanceWheels
	{
		mesh = vehicles/Vehicles_90fordF350ambulance_Wheels|f350_tires,
		texture = vehicles/Vehicles_90fordF350ambulance_Wheel,
		shader = vehiclewheel,
		scale = 0.97,
	}


	template vehicle F350Tires
	{
		part TireFrontLeft
		{
			model F350TireFL
			{
				file = 90fordF350ambulanceWheel,
			}

			itemType = Base.90fordF350Tire,
			area = TireFrontLeft,
			wheel = FrontLeft,
			table install
			{
				requireInstalled = BrakeFrontLeft;SuspensionFrontLeft,
			}

		}

		part TireFrontRight
		{
			model F350TireFR
			{
				file = 90fordF350ambulanceWheel,
			}

			itemType = Base.90fordF350Tire,
			area = TireFrontRight,
			wheel = FrontRight,
			table install
			{
				requireInstalled = BrakeFrontRight;SuspensionFrontRight,
			}
		}

		part TireRearLeft
		{
			model F350TireRL
			{
				file = 90fordF350ambulanceWheels,
			}

			itemType = Base.90fordF350DoubleTires,
			area = TireRearLeft,
			wheel = RearLeft,
			table install
			{
				requireInstalled = BrakeRearLeft;SuspensionRearLeft,
			}
		}

		part TireRearRight
		{
			model F350TireRR
			{
				file = 90fordF350ambulanceWheels,
			}

			itemType = Base.90fordF350DoubleTires,
			area = TireRearRight,
			wheel = RearRight,
			table install
			{
				requireInstalled = BrakeRearRight;SuspensionRearRight,
			}
		}

		part Tire*
		{
			category = tire,
			container
			{
				capacity = 35,
				test = Vehicles.ContainerAccess.Tire,
				contentType = Air,
			}
			table install
			{
				items
				{
					1
					{
						type = Base.Jack,
						count = 1,
						keep = true,
					}
					2
					{
						type = Base.LugWrench,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 400,
				skills = Mechanics:1,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Tire,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Jack,
						count = 1,
						keep = true,
					}
					2
					{
						type = Base.LugWrench,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 400,
				skills = Mechanics:1,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Tire,
			}

			lua
			{
				create = Vehicles.Create.Tire,
				init = Vehicles.Init.Tire,
				checkOperate = Vehicles.CheckOperate.Tire,
				update = Vehicles.Update.Tire,
			}
		}
	}
}

